{
  "devDependencies": {
    "daisyui": "^4.12.13",
    "tailwindcss": "^3.4.14"
  },
  "scripts": {
    "build": "npm run build:css && npm run build:rs",
    "build:css": "npx tailwindcss -i ./tailwind.css -o ./static/tailwind.css --watch --minify",
    "build:rs": "trunk build --release",
    "dev": "trunk serve"
  }
}
